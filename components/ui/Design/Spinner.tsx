"use client";

import CircularProgress from '@mui/material/CircularProgress';

const Spinner = () => {
  return <CircularProgress size={20} sx={{ color : "rgba(45,212,191,1)" }}/>
}

export default Spinner