"use client";

import CircularProgress from '@mui/material/CircularProgress';

const DeleteSpinner = () => {
  return <CircularProgress size={20} sx={{ color : "rgba(0, 0, 0, 1)" }}/>
}

export default DeleteSpinner